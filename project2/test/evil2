#pragma source off
#pragma token off
#pragma statistic off

// error
int a(int a[2][8], double z[]) {
	return 0;
}

int a();

int main() {
	if (3.14){}
	if ("sss"){}

	// error
	string ss() {

	}

	// error
	void b();

	// error
	if(true){
		else{}
	}
	
}